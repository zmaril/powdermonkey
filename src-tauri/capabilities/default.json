{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main window. The UI only needs the webview's own network access (fetch/WebSocket to the chosen supervisor), which CSP gates — see tauri.conf.json's security.csp — so no extra plugin permissions are required here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
