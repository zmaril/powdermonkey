{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main window. The UI needs the webview's own network access (fetch/WebSocket to the chosen supervisor), which CSP gates — see tauri.conf.json's security.csp — plus the opener plugin so external links (terminal URLs, PR/review links) open in the OS default browser rather than the webview.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
