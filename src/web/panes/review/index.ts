// The in-app PR review pane — a PR's diff with inline review comments, threaded and
// submitted as one GitHub review. One component per file; ReviewPane is the entry.
export { ReviewPane } from "./ReviewPane.tsx";
